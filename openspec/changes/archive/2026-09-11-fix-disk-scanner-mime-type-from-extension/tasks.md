## 1. Auditoría y preparación del writer

- [x] 1.1 Auditar todos los callers de `DiskScannerService` con `grep -rn "new DiskScannerService" app/` y confirmar que ninguno instancia la clase con args posicionales (todos pasan por container o solo lo invocan vía Laravel facade). **Resultado**: 0 instancias manuales; único caller con DI es `ScanAndSubmitCommand` vía `handle()` method-injection. Constructor change es seguro.
- [x] 1.2 Confirmar que `FileScannerService` ya está disponible en el container de Laravel (debe estarlo, dado su uso en `StorageSyncService`). Si no, registrarlo en un service provider. **Resultado**: sin service provider explícito; auto-resuelto igual que en `StorageSyncService`. Ningún cambio necesario.
- [x] 1.3 Ejecutar `EXPLAIN SELECT id, name, mime_type FROM files WHERE mime_type='video/mp4' AND name ~* '\.(mp3|m4a|opus|flac|wav|aac)$'` para verificar el plan de la query del reconciliador. Si el plan no usa índice, proponer migración `CREATE INDEX CONCURRENTLY` (fuera de este change) antes del runbook. **Resultado**: Seq Scan (acceptable para 1.86M filas). Duración dry-run = 7s con chunk=5000. No se necesita índice nuevo. **Datos del bug**: total_rows=1,861,749 / video_mp4_rows=443,547 / audio_mislabeled_rows=43,602.

## 2. Fix del writer (DiskScannerService)

- [x] 2.1 En `app/app/Services/Ia/DiskScannerService.php`, agregar `private FileScannerService $scanner` al constructor (patrón como `StorageSyncService.php:18`) y asignar `$this->scanner = $scanner` en el cuerpo.
- [x] 2.2 Reemplazar el literal `'mime_type' => 'video/mp4'` en línea 237 por `$this->scanner->getMimeType($c['name'])`.
- [x] 2.3 Verificar que `getMimeType()` ya cubre las extensiones que el filtro del scanner acepta (`.mp4`, `.mkv`, `.mp3`, `.opus`, `.flac`, `.wav`, `.aac`). Confirmar leyendo `app/app/Services/FileScannerService.php:147-200`. Si faltara `.opus`, agregarlo al mapa (`audio/opus`). **Resultado**: faltaba `.opus`. Agregado en `FileScannerService.php` línea 168: `'opus' => 'audio/opus'`.
- [x] 2.4 Confirmar que NO existe otra rama del método `scanStorage()` o de `ensureFile()` que escriba `mime_type` hardcoded. `grep -n "video/mp4" app/app/Services/Ia/DiskScannerService.php` debe devolver solo la línea 634 (caso carpeta, no archivo). **Resultado**: 0 ocurrencias de `video/mp4` en el archivo (línea 634 era `'mime_type' => 'folder'` que es folder, no el bug).

## 3. Comando artisan de reconciliación

- [x] 3.1 Crear `app/app/Console/Commands/Avisos/ReconcileFileMimeTypes.php` con signature `avisos:reconcile-file-mime-types`. Namespace `App\Console\Commands\Avisos`. Co-locado con los demás comandos de avisos. **Resultado**: creado en `app/app/Console/Commands/ReconcileFileMimeTypesCommand.php` (namespace flat, el patrón del proyecto — los demás comandos de avisos viven en `Console/Commands/` no en subcarpetas).
- [x] 3.2 Implementar opción `--apply` (default dry-run). La opción `--chunk` opcional (default 500) para tests de stress.
- [x] 3.3 Implementar la query candidata: `SELECT id, name FROM files WHERE mime_type='video/mp4' AND name ~* '\.(mp3|m4a|opus|flac|wav|aac)$' ORDER BY id ASC`. Paginar con `LIMIT $chunk OFFSET $offset` (offset sobre IDs ascendentes para no rescanear filas ya tocadas en runs previos). **Implementación real**: cursor-style con `WHERE id > $lastId` (más rápido que OFFSET; respeta `no_consultas_pesadas_masivas_servidor`).
- [x] 3.4 Para cada chunk: mapear la extensión del `name` al mime correcto (reusar `FileScannerService::getMimeType()`). Ejecutar `UPDATE files SET mime_type = ? WHERE id IN (...)` con bind de IDs del chunk. **Implementación real**: agrupa IDs por mime destino, ejecuta un UPDATE por mime. Verificado.
- [x] 3.5 Loggear con `Log::info(...)` cada paso: `files.mime_reconcile.started` (con `apply`, `chunk`, `dry_run`), `files.mime_reconcile.chunk` (con `updated`, `candidates`), `files.mime_reconcile.finished` (con `total_updated`, `total_skipped_unknown_extension`, `duration_ms`). **Implementación real**: `Log::warning(...)` porque `LOG_LEVEL=warning` en producción filtra `Log::info` (verificado leyendo `laravel.log`).
- [x] 3.6 Al final del comando (con o sin `--apply`), imprimir en stdout el conteo y la línea "Estimado: K filas se reclasificarán de TV → Radio en Mis Avisos tras la próxima carga." (donde K es `total_updated`).
- [x] 3.7 Registrar el comando en `app/app/Console/Kernel.php` (o el mecanismo que use el proyecto para auto-descubrir comandos en `Console/Commands/Avisos/`; verificar el patrón actual con los comandos existentes del módulo). **Resultado**: Laravel 11+ auto-descubre comandos en `app/app/Console/Commands/`. Verificado con `php artisan list | grep mime` → comando aparece sin registro manual.

## 4. Harness de regresión

- [x] 4.1 Crear `app/tests/harness_disk_scanner_mime_type.php` siguiendo el patrón de `app/tests/harness_mis_avisos_viewer.php` (tag único `hdms_<8-hex>`, helpers `h_ok`/`h_fail`/`h_check`, contador global `$failures`, cleanup defensivo al inicio con `LIKE 'hdms_%'`, finally con cleanup en orden inverso).
- [x] 4.2 Aserción (a): crear un storage temporal con `transcription_enabled=true` y `folder_layout='flat'`. Crear archivos físicos `.mp3`, `.opus`, `.wav`, `.mp4` en una carpeta temporal dmY. Llamar a `DiskScannerService::scanStorage()` con el storage. Confirmar que las filas `files` resultantes tienen `mime_type='audio/mpeg'`, `'audio/opus'`, `'audio/wav'`, `'video/mp4'` respectivamente. **Resultado**: 4/4 mime_types correctos.
- [x] 4.3 Aserción (b): invocar `MentionsSearchService::classifyMediaKind($file->mime_type)` para cada fila y confirmar que `.mp3/.opus/.wav` devuelven `'radio'` y `.mp4` devuelve `'tv'`. **Resultado**: 4/4 clasificaciones correctas.
- [x] 4.4 Aserción (c): preparar 3 filas `files` con `mime_type='video/mp4'` y `name='hdms_xxx_test.mp3'` (con prefijo hdms para limpieza segura). Ejecutar `Artisan::call('avisos:reconcile-file-mime-types')` (sin `--apply`). Confirmar que las filas NO se mutaron y que la salida contiene el conteo esperado. **Resultado**: 0 mutaciones; salida contiene "DRY-RUN" y conteo.
- [x] 4.5 Aserción (d): ejecutar `Artisan::call('avisos:reconcile-file-mime-types', ['--apply' => true])`. Confirmar que las 3 filas quedaron con `mime_type='audio/mpeg'` y que el log `laravel.log` contiene `files.mime_reconcile.finished` con `total_updated=3`. **Resultado**: 3/3 filas mutadas, log verificable.
- [x] 4.6 Aserción (e): crear 2 filas `files` con `name='hdms_xxx_video.mp4'` y `mime_type='video/mp4'`. Ejecutar `--apply`. Confirmar que esas filas NO se modifican (siguen siendo `video/mp4`). **Resultado**: 2/2 archivos .mp4 intactos.
- [x] 4.7 (Aserción extra idempotencia): re-ejecutar `--apply`; confirmar que el segundo run no produce mutaciones adicionales. Incluida para validar el comportamiento "sin trabajo adicional tras la primera corrida exitosa". **Resultado**: idempotente (verificado, queda <=3 video/mp4 tras re-run).

## 5. Runbook de validación pre/post deploy (acciones del operador)

- [ ] 5.1 **Commit del writer fix + comando + harness** (3 archivos modificados + 1 nuevo + 1 nuevo harness). Correr `cd app && php tests/harness_disk_scanner_mime_type.php` localmente antes de commit. **Ya validado: harness exit 0 localmente**.
- [ ] 5.2 **Commit + push + merge**. En el servidor, `php artisan config:cache` para limpiar opcache de PHP-FPM. Acción del operador.
- [ ] 5.3 **Verificar writer nuevo en vivo**: ejecutar `php artisan transcription:scan-and-submit --days=0` sobre un storage de radio conocido (ej: storage de la `01 Radio FM Bogota`/`10 Alerta Bogota`). Confirmar en BD con `psql` que filas nuevas de `.mp3` se persisten con `mime_type='audio/mpeg'`. Acción del operador post-deploy.
- [ ] 5.4 **Dry-run del reconciliador**: `php artisan avisos:reconcile-file-mime-types`. **Ya validado en dev**: 43,602 candidatos en producción. Si el conteo es razonable, proceder.
- [ ] 5.5 **Apply del reconciliador** (ACCIÓN DESTRUCTIVA SOBRE BD DE PRODUCCIÓN): `php artisan avisos:reconcile-file-mime-types --apply`. Confirmar logs `files.mime_reconcile.*` en `laravel.log`. **NO EJECUTADO todavía** — decisión del operador (cambia 43.602 filas en BD de producción).
- [ ] 5.6 **Validación visual con el cliente real que motivó el reporte**: cargar Mis Avisos del cliente, confirmar que emisoras de radio (Radio FM Bogota, Alerta Bogota, etc.) ahora muestran ícono ámbar `fa-radio` en vez del marquetilla violeta de TV. Capturar screenshot de comparación pre/post. Acción del operador o cliente.
- [ ] 5.7 **Re-correr el harness en servidor**: `cd app && php tests/harness_disk_scanner_mime_type.php`. Exit 0. Acción del operador (sanity, no obligatorio si el local ya pasó).
- [ ] 5.8 **Monitoreo post-apply durante 24h**: `grep "files.mime_reconcile" storage/logs/laravel.log` para confirmar que no hay errores del reconciliador. `grep -c "files.mime_reconcile" storage/logs/laravel.log` debería mostrar al menos `started` + cada `chunk` + `finished`. Acción del operador.

